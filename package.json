{
  "private": true,
  "scripts": {
    "start": "elm-test --watch ./tests/Tests.elm",
    "format": "elm-format --yes src tests answers"
  },
  "devDependencies": {
    "elm": "^0.19.1",
    "elm-format": "^0.8.5",
    "elm-test": "^0.19.1"
  }
}
